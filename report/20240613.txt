今天下午去跟宪哥讨论了一下，接下去要做的事无非就是以下几点：

1. 解决import mathlib的事情（尚未解决，可能跟path之类的有关，需要明天去处理）

2. 对每个example都进行语法问题的修复，最后进行整合（暂时搞定）

3. 修改prompt，使其能够在本来的library的基础上能够有一定的延伸，但也不至于太脱离原先的lib

4. 让每一个example生成的lib有个标识符，这样在处理的时候有个明确的指向对象（通过给example排序号可能可以搞定）