今天继续昨天的任务，即把写好的框架装进代码之中

检查syntax error的部分效果非常不错，然而我代码总是识别不了mathlib导致总是会报错，这个的话可能在别人的电脑那边可以解决

现在的问题是，先处理solution的答案错误还是先处理语法问题；

先处理solution答案的话，好处就是能保证数学正确之后再处理语法，坏处就是处理语法得要把整个library投给GPT4，最后返回给我们的可能只有一小部分library（当然有一个办法就是在生成library的时候就进行语法处理，不过时间复杂度会变大？）

先处理语法，相当于把整个library投给GPT4，和前面同理，不会返回完整内容（可以prompt明确说，但是怪怪的）好处好像没有，因为修改答案的过程也会改变语法，到头来还是得修改语法

宪哥今天好像不在的样子，所以我就先回家了